arq = open('ArquivoBi.bin', 'wb')
# Numero = int(input('Informe um numero inteiro: '))
# arq.write(Numero.to_bytes(3, byteorder='little', signed=True))
# Numero = Numero.to_bytes(3, byteorder='little', signed=True) #Armazenando numero convertido em bytes
# x = int.from_bytes(Numero, byteorder='little', signed=True) #Convertendo para decimal novamente o numero convertido em bytes
# print(x)
# arq.close()